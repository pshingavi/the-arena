[phases.setup]
nixPkgs = ["python311", "uv", "gcc", "libstdcxx5"]

[phases.install]
cmds = ["uv pip install --system -r requirements.txt"]

[start]
cmd = "uvicorn main:app --host 0.0.0.0 --port $PORT"
